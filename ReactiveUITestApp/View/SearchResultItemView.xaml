<UserControl x:Class="ReactiveUITestApp.View.SearchResultItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" x:Name="DoubleClickCommand" />
            </Grid.InputBindings>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="Name" />
            <TextBlock Grid.Column="1" x:Name="Size" />
            <TextBlock Grid.Column="2" x:Name="Score" />
        </Grid>
    </Grid>
</UserControl>
